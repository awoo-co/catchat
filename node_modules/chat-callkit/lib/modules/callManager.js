"use strict";

exports.__esModule = true;
exports.client = exports.callManager = void 0;

var _regenerator = _interopRequireDefault(require("@babel/runtime/regenerator"));

var _agoraRtcSdkNg = _interopRequireDefault(require("agora-rtc-sdk-ng"));

var _agoraChat = _interopRequireDefault(require("agora-chat"));

exports.WebIM = _agoraChat["default"];

var _redux = _interopRequireDefault(require("../redux"));

var _reducer = require("../redux/reducer");

var _message = require("./message");

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

var client = _agoraRtcSdkNg["default"].createClient({
  mode: 'live',
  codec: 'h264'
});

exports.client = client;
client.setClientRole('host');

var Manager = /*#__PURE__*/function () {
  function Manager(params) {
    if (params) {
      this.init(params);
    }

    this.client = client;
    this.props = {};
    this.callId = '';
    _agoraChat["default"].rtc = this.rtc = {
      client: client,
      localAudioTrack: null,
      localVideoTrack: null,
      timer: null
    };
  }

  var _proto = Manager.prototype;

  _proto.init = function init(appId, agoraUid, connection) {
    this.appId = appId;
    this.agoraUid = agoraUid;
    _agoraChat["default"].conn = connection;
    (0, _message.addListener)();
  };

  _proto.setCallKitProps = function setCallKitProps(props) {
    this.props = props;
  };

  _proto.changeState = function changeState(state) {
    this.props.onStateChange && this.props.onStateChange(state);
  };

  _proto.setToken = function setToken(accessToken) {
    this.accessToken = accessToken;
  };

  _proto.setUserIdMap = function setUserIdMap(idMap) {
    var getState = _redux["default"].getState,
        dispatch = _redux["default"].dispatch;
    dispatch((0, _reducer.setUidToUserId)(idMap));
  };

  _proto.answerCall = function answerCall(result, accessToken) {
    var getState = _redux["default"].getState,
        dispatch = _redux["default"].dispatch;
    var state = getState();
    var confr = state.confr;

    if (result) {
      this.accessToken = accessToken;
      (0, _message.sendAlerting)(confr.callerIMName, confr.callerDevId, confr.callId);
    } else {
      var _dispatch = _redux["default"].dispatch;

      _dispatch((0, _reducer.setCallStatus)(_reducer.CALLSTATUS.idle));
    }
  };

  _proto.startCall = function startCall(options) {
    var getState = _redux["default"].getState,
        dispatch = _redux["default"].dispatch;
    var state = getState();
    var callId = options.callId,
        channel = options.channel,
        chatType = options.chatType,
        callType = options.callType,
        to = options.to,
        message = options.message,
        groupId = options.groupId,
        groupName = options.groupName,
        accessToken = options.accessToken;
    this.accessToken = accessToken;

    if (state.confr.callId && state.callStatus > 0) {
      channel = state.confr.channel;
      callId = state.confr.callId;
      callType = state.confr.type;
    }

    var confInfo = {
      action: 'invite',
      channelName: channel,
      type: callType,
      //0 1v1 audio，1 1v1 video，2 multi video 3 multi audio
      callerDevId: _agoraChat["default"].conn.context.jid.clientResource,
      callId: callId,
      ts: Date.now(),
      msgType: 'rtcCallWithAgora',
      callerIMName: _agoraChat["default"].conn.context.jid.name,
      // for ios push
      chatType: callType,
      em_push_ext: {
        type: 'call',
        custom: {
          callId: callId
        }
      },
      em_apns_ext: {
        em_push_type: 'voip'
      }
    };
    var msgExt = {
      channelName: channel,
      token: null,
      type: callType,
      callerDevId: _agoraChat["default"].conn.context.jid.clientResource,
      callId: callId,
      calleeIMName: to,
      callerIMName: _agoraChat["default"].conn.context.jid.name
    };
    this.callId = callId;

    if (callType === 2 || callType === 3) {
      confInfo.ext = {
        groupId: groupId,
        groupName: groupName
      };
      msgExt.ext = {
        groupId: groupId,
        groupName: groupName
      };
      var invitedMembers = state.invitedMembers;

      if (!invitedMembers.includes(to)) {
        dispatch((0, _reducer.updateInvitedMembers)([].concat(invitedMembers, [to])));
      }
    }

    if (chatType === 'groupChat') {// confInfo.ext = {
      // 	groupId: groupId,
      // 	groupName: groupName
      // }
      // msgExt.ext = {
      // 	groupId: groupId,
      // 	groupName: groupName
      // }
      // let invitedMembers = state.invitedMembers
      // dispatch(updateInvitedMembers([...invitedMembers, to]))
      // // sendCMDMsg(chatType, to, message, confInfo)
      // sendTextMsg(chatType, to, message, confInfo)
    } else {
      (0, _message.sendTextMsg)(chatType, to, message, confInfo);
    }

    dispatch((0, _reducer.updateConfr)({
      ext: msgExt,
      to: to,
      from: _agoraChat["default"].conn.context.jid.name
    }));

    if (state.callStatus < _reducer.CALLSTATUS.inviting) {
      dispatch((0, _reducer.setCallStatus)(_reducer.CALLSTATUS.inviting));
    }
  };

  _proto.join = /*#__PURE__*/function () {
    var _join = _asyncToGenerator( /*#__PURE__*/_regenerator["default"].mark(function _callee() {
      var getState, dispatch, state, confr, username, uid, localAudioTrack, config, displayedName, localVideoTrack, videoElm;
      return _regenerator["default"].wrap(function _callee$(_context) {
        while (1) {
          switch (_context.prev = _context.next) {
            case 0:
              getState = _redux["default"].getState, dispatch = _redux["default"].dispatch;
              state = getState();

              if (!(state.callStatus === _reducer.CALLSTATUS.confirmCallee)) {
                _context.next = 4;
                break;
              }

              return _context.abrupt("return");

            case 4:
              confr = state.confr;
              username = _agoraChat["default"].conn.context.userId;
              _context.next = 8;
              return client.join(this.appId, confr.channel, this.accessToken, Number(this.agoraUid));

            case 8:
              uid = _context.sent;
              _context.next = 11;
              return _agoraRtcSdkNg["default"].createMicrophoneAudioTrack();

            case 11:
              localAudioTrack = _context.sent;
              this.rtc.localAudioTrack = localAudioTrack;
              config = [localAudioTrack];
              displayedName = username;

              if (state.uid2userId[uid]) {
                displayedName = state.uid2userId[uid];
              } else if (state.uid2userId[username]) {
                displayedName = state.uid2userId[username];
              }

              if (!(confr.type === 0 || confr.type === 3)) {
                _context.next = 22;
                break;
              }

              _context.next = 19;
              return client.publish(config);

            case 19:
              dispatch((0, _reducer.updateJoinedMembers)({
                videoElm: null,
                name: displayedName,
                type: 'audio',
                value: uid,
                action: 'add',
                audio: true,
                video: false,
                isSelf: true
              }));
              _context.next = 32;
              break;

            case 22:
              _context.next = 24;
              return _agoraRtcSdkNg["default"].createCameraVideoTrack();

            case 24:
              localVideoTrack = _context.sent;
              config.push(localVideoTrack);
              this.rtc.localVideoTrack = localVideoTrack;
              _context.next = 29;
              return client.publish(config);

            case 29:
              videoElm = confr.type === 2 ? 'video' + uid : 'local-player';
              dispatch((0, _reducer.updateJoinedMembers)({
                videoElm: videoElm,
                name: displayedName,
                type: 'video',
                value: uid,
                action: 'add',
                audio: true,
                video: true,
                isSelf: true
              }));
              localVideoTrack.play(videoElm);

            case 32:
              this.startTime();

            case 33:
            case "end":
              return _context.stop();
          }
        }
      }, _callee, this);
    }));

    function join() {
      return _join.apply(this, arguments);
    }

    return join;
  }();

  _proto.startTime = function startTime() {
    var dispatch = _redux["default"].dispatch;
    var hour = 0,
        minute = 0,
        second = 0;
    this.rtc.intervalTimer && clearInterval(_agoraChat["default"].rtc.intervalTimer);
    var timerId = setInterval(function () {
      second += 1;

      if (second === 60) {
        second = 0;
        minute += 1;

        if (minute === 60) {
          minute = 0;
          hour += 1;

          if (hour == 24) {
            hour = 0;
          }
        }
      }

      var time = (0, _utils.formatTime)(hour, minute, second);
      dispatch((0, _reducer.setCallDuration)(time));
    }, 1000);
    this.rtc.intervalTimer = timerId;
  };

  _proto.hangup = /*#__PURE__*/function () {
    var _hangup = _asyncToGenerator( /*#__PURE__*/_regenerator["default"].mark(function _callee2(reson, isCancel) {
      var getState, dispatch, state, confr;
      return _regenerator["default"].wrap(function _callee2$(_context2) {
        while (1) {
          switch (_context2.prev = _context2.next) {
            case 0:
              this.rtc.localAudioTrack && this.rtc.localAudioTrack.close();
              this.rtc.localVideoTrack && this.rtc.localVideoTrack.close();
              this.rtc.intervalTimer && clearInterval(this.rtc.intervalTimer);
              getState = _redux["default"].getState, dispatch = _redux["default"].dispatch;
              state = getState();
              confr = state.confr;

              if (isCancel && confr.callerIMName == _agoraChat["default"].conn.context.jid.name) {
                if (confr.type == 2 || confr.type == 3) {
                  state.invitedMembers.forEach(function (member) {
                    (0, _message.cancelCall)(member);
                  });
                } else {
                  (0, _message.cancelCall)();
                }
              }

              this.props.onStateChange && this.props.onStateChange({
                type: 'hangup',
                reson: reson,
                callInfo: _extends({}, state.confr, {
                  duration: state.callDuration,
                  groupId: state.groupId,
                  groupName: state.groupName
                })
              });
              this.callId = '';
              _context2.t0 = this.rtc.client;

              if (!_context2.t0) {
                _context2.next = 13;
                break;
              }

              _context2.next = 13;
              return this.rtc.client.leave();

            case 13:
              dispatch((0, _reducer.setCallStatus)(_reducer.CALLSTATUS.idle));
              dispatch((0, _reducer.setCallDuration)('00:00'));
              dispatch((0, _reducer.changeWinSize)('normal'));
              dispatch((0, _reducer.updateJoinedMembers)([]));
              dispatch((0, _reducer.updateInvitedMembers)([]));
              dispatch((0, _reducer.updateConfr)({
                to: '',
                ext: {}
              }));

            case 19:
            case "end":
              return _context2.stop();
          }
        }
      }, _callee2, this);
    }));

    function hangup(_x, _x2) {
      return _hangup.apply(this, arguments);
    }

    return hangup;
  }();

  return Manager;
}();

var callManager = new Manager();
exports.callManager = callManager;